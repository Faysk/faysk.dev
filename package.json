{
  "name": "faysk-dev",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro check && astro build",
    "preview": "astro preview --host 127.0.0.1",
    "check": "astro check"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "overrides": {
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "astro": "^6.0.0",
    "typescript": "^5.9.0"
  }
}
