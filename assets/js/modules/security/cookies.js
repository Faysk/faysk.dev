
import { createSection, addItem } from '../utils.js';

export function initCookies(){

  const section =
  createSection(
    'cookies',
    '🚀 cookies'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
