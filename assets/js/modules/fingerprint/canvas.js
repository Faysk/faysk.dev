
import { createSection, addItem } from '../utils.js';

export function initCanvas(){

  const section =
  createSection(
    'canvas',
    '🚀 canvas'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
