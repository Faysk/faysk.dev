
import { createSection, addItem } from '../utils.js';

export function initFingerprint(){

  const section =
  createSection(
    'fingerprint',
    '🚀 fingerprint'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
