
import { createSection, addItem } from '../utils.js';

export function initWebgl(){

  const section =
  createSection(
    'webgl',
    '🚀 webgl'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
