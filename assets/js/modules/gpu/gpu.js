
import { createSection, addItem } from '../utils.js';

export function initGpu(){

  const section =
  createSection(
    'gpu',
    '🚀 gpu'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
