
import { createSection, addItem } from '../utils.js';

export function initBrowser(){

  const section =
  createSection(
    'browser',
    '🚀 browser'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
