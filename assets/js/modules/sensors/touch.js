
import { createSection, addItem } from '../utils.js';

export function initTouch(){

  const section =
  createSection(
    'touch',
    '🚀 touch'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
