
import { createSection, addItem } from '../utils.js';

export function initOrientation(){

  const section =
  createSection(
    'orientation',
    '🚀 orientation'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
