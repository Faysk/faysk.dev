
import { createSection, addItem } from '../utils.js';

export function initTiming(){

  const section =
  createSection(
    'timing',
    '🚀 timing'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
