
import { createSection, addItem } from '../utils.js';

export function initBenchmark(){

  const section =
  createSection(
    'benchmark',
    '🚀 benchmark'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
