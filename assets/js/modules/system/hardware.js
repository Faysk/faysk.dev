
import { createSection, addItem } from '../utils.js';

export function initHardware(){

  const section =
  createSection(
    'hardware',
    '🚀 hardware'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
