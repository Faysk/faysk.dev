
import { createSection, addItem } from '../utils.js';

export function initMemory(){

  const section =
  createSection(
    'memory',
    '🚀 memory'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
