
import { createSection, addItem } from '../utils.js';

export function initWebrtc(){

  const section =
  createSection(
    'webrtc',
    '🚀 webrtc'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
