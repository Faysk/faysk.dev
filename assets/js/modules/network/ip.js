
import { createSection, addItem } from '../utils.js';

export function initIp(){

  const section =
  createSection(
    'ip',
    '🚀 ip'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
