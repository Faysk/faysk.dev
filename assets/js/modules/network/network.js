
import { createSection, addItem } from '../utils.js';

export function initNetwork(){

  const section =
  createSection(
    'network',
    '🚀 network'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
