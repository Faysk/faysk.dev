
import { createSection, addItem } from '../utils.js';

export function initTerminal(){

  const section =
  createSection(
    'terminal',
    '🚀 terminal'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
