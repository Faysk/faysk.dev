
import { createSection, addItem } from '../utils.js';

export function initCharts(){

  const section =
  createSection(
    'charts',
    '🚀 charts'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
