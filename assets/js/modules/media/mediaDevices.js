
import { createSection, addItem } from '../utils.js';

export function initMediaDevices(){

  const section =
  createSection(
    'mediadevices',
    '🚀 mediaDevices'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
