
import { createSection, addItem } from '../utils.js';

export function initVideoDevices(){

  const section =
  createSection(
    'videodevices',
    '🚀 videoDevices'
  );

  addItem(
    section,
    'Status',
    'Module initialized'
  );

}
